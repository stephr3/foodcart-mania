import { starDisplay } from 'foodcart-mania/helpers/star-display';
import { module, test } from 'qunit';

module('Unit | Helper | star display');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = starDisplay([42]);
  assert.ok(result);
});
